/**
 * Reemplazar el "/api/" dependiendo de la carpeta en la que se 
 * encuentre el proyecto.
 */
// const domain = window.location.host + "/api/";
// const pro = window.location.protocol;

// export const url = `${pro}//${domain}`;
export const url = `${window.location.origin}/api/`;
